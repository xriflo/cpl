import ro.pub.cs.lcpl.*;

public class LCPLInterpreter {
	public void run(Program p)
	{
		/*** Your code begins here ***/
		System.out.print("Hello world!\n");
		
		/* TO DO:
		 * Set up the data structures needed to evaluate a program
		 *   a symbol table to locate classes, attributes, methods, parameters, variables
		 *   an object heap to store new objects
		 *   a stack to store parameters and local variables
		 * Locate the LCPLClass object representing "Main"
		 * Locate the Method object representing the method "main"
		 * Create a new object of LCPL type "Main"
		 * Evaluate a dispatch to "Main.main"
		 * */
		
		/*** Your code ends here ***/
	}
}
